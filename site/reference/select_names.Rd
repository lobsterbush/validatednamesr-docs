% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_names.R
\name{select_names}
\alias{select_names}
\title{Select names}
\usage{
select_names(race, pct_correct = 0.8, order_by_var = NULL, n_names = 5)
}
\arguments{
\item{race}{Character. The race intended to be signaled by the names. The value should be either "Asian," "Hispanic," "Black," or "White."}

\item{pct_correct}{Numeric. Minimum share who perceived the intended racial signal. Defaults to 0.8.}

\item{order_by_var}{Character. The variable to be used to order the list of the names to be extracted. The default variable is NULL (non-selected). In this case, the names will be chosen randomly. The other options are pct_correct = the percentage of the name's intended race correctly perceived, avg_income = average perceived income level, avg_education = average perceived education level, and avg_citizenship = the percentage of perceived citizenship status.}

\item{n_names}{Numeric. Number of names to select. Defaults to five; ordering keeps ties and can return more.}
}
\value{
A data frame with name, identity, pct_correct, avg_income, avg_education, and avg_citizenship.
}
\description{
Choose names whose intended racial signal was perceived by at least the requested share of respondents. You can sample eligible names or select those with the highest values on a validation measure.
}
